/**
 * _abs - the function name
 * @n: parameter to return absolute value
 *
 * * Description: To get an absolute of an integer
 *
 * Return: Always 0 (success)
 */
int _abs(int n)
{
	if (n  < 0)
		n = (-1) * n;
	return (n);
}
