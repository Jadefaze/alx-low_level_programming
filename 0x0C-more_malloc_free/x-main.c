#include "main.h"
#include <stdio.h>

/**
 * main: to test string func
 * Return: 0
 */

int main(void)
{
	char *str;

	str = "Jadefaze";

	printf("The length of the string is: %d\n", _str_len(str));

	return (0);
}
