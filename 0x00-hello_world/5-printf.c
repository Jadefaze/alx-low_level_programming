#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char mytext[] = "with proper grammer, but the outcome is a piece of art,";

	printf("%s", mytext);
	return (0);
}
